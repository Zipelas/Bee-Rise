class Button {
    private title: string;
    private backgroundColor: string;
    private width: number;
    private height: number;
    private textColor: string;
    private image: p5.Image;
    private x: number;
    private y: number;

    constructor(title: string, backgroundColor: string, width: number, height: number, textColor: string, image: p5.Image, x: number, y: number) {
        this.title = title;
        this.backgroundColor = backgroundColor;
        this. width = width;
        this.height = height;
        this.textColor = textColor;
        this.image = image;
        this.x = x;
        this.y = y;
        }

        public update() {

        }

        public draw() {

        }

        public onclick() {
            
        }
}