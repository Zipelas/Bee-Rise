/// <reference path="entity.ts" />

class Moln extends Entity {
  constructor(x: number, y: number, width: number, height: number, velocityX: number, image: p5.Image) {
    super(x, y, width, height, velocityX, 0, image); 
  }

  public update() {
    
  }
}